CREATE TABLE fornecedor(
    cnpj varchar PRIMARY KEY UNIQUE NOT NULL,
    razaoSocial TEXT NOT NULL,
    nomeFantasia TEXT,
    endereco VARCHAR(255),
    cidade VARCHAR(255),
    cep VARCHAR(9),
    fone INTEGER,
    celular1 INTEGER NOT NULL,
    celular2 INTEGER,
    email VARCHAR (255)
    );